process {
    withLabel: bwa { cpus = params.cores ; memory = params.memory } 
    withLabel: concoct { cpus = params.cores ; memory = params.memory }
    withLabel: fastp { cpus = params.cores ; memory = params.memory  }
    withLabel: filtlong { cpus = params.cores ; memory = params.memory  }
    withLabel: flye { cpus = params.cores ; memory = params.memory  }
    withLabel: maxbin { cpus = params.cores ; memory = params.memory  }  
    withLabel: medaka { cpus = params.cores ; memory = params.memory  } 
    withLabel: metabat { cpus = params.cores ; memory = params.memory  }  
    withLabel: metawrap { cpus = params.cores ; memory = params.memory  } 
    withLabel: minimap { cpus = params.cores ; memory = params.memory  }
    withLabel: checkm { cpus = params.cores ; memory = params.memory  }
    //withLabel: checkm(withLabel: metawrap)
    withLabel: pilon { cpus = params.cores ; memory = params.memory } 
    withLabel: racon { cpus = params.cores ; memory = params.memory  } 
    withLabel: seqtk { cpus = params.cores ; memory = params.memory  } 
    withLabel: sourmash { cpus = params.cores ; memory = params.memory  }
    withLabel: spades { cpus = params.cores ; memory = params.memory   }
    withLabel: ubuntu { cpus = params.cores ; memory = params.memory  } 
    withLabel: unicycler { cpus = params.cores ; memory = params.memory  }
    withLabel: eggnog { cpus = params.cores ; memory = params.memory  }
    withLabel: trinity { cpus = params.cores ; memory = params.memory  }
}
