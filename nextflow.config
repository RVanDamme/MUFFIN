manifest {
    mainScript = './main.nf'
}

// default parameters
params {
    cores = "1"
    memory = '12'
    help = false
    profile = false

    // Inputs (considered as dir)
    ont = './nanopore'
    illumina = './illumina'

    // Databases
    checkm_db = false
    checkm_tar_db = false
    sour_db = false

    // Options
    skip_ill_qc = false
    skip_ont_qc = false
    short_qc = "2000"
    filtlong = false
    polish_threshold = ""
    polish_iteration = 3
    skip_metabat2 =false
    skip_maxbin2 =false
    skip_concoct =false

    // Output
    output = './results'
}

profiles {
    standard {

    }

    conda {
        process.executor = 'conda'
        docker.enabled=false
        process {
            withLabel : fastp { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::fastp'}
            withLabel : filtlong { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::filtlong'}
            withLabel : sourmash { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::sourmash'}
            withLabel : spades { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::spades'}
            withLabel : flye { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::flye'}
            withLabel : pilon { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::pilon '}
            withLabel : minimap2 { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::minimap2 bioconda::samtools'}
            withLabel : bwa { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::bwa bioconda::samtools'}
            withLabel : metabat2 { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::metabat2'}
            withLabel : maxbin2 { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::maxbin2'}
            withLabel : concoct { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::concoct'}
            withLabel : checkm { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::checkm-genome'}
            withLabel : metawrap { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::metawrap '}
            withLabel : unicycler { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::unicycler '}
            withLabel : seqtk { cpus = param.cores ; memory = param.memory
            conda = 'bioconda::seqtk '}
            // withLabel : { cpus = param.cores ; memory = param.memory
            // conda = 'bioconda:: '}
        }
    }

}